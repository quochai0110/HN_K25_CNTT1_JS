/* 

    Các kiểu dữ liệu trong JS
    2 NHÓM kiểu dữ liệu :
    Nhóm I: kiểu dữ liệu nguyên thủy, primitive type, tham trị
        1. Number
        2. String
        3. Boolean
        4. null
        5. undefined
        6. NaN
        ... Symbol
    Nhóm II: kiểu dữ liệu phức tạp, reference type, tham chiếu
        1. array
        2. function
        3. object
*/
// Kiểu dữ liệu number
let age = 20;
let price = 10.0;
let quantity = 5;

// Kiểu dữ liệu string
let fullName = " Lê Minh Thu";

// Kiểu dữ liệu null
let bookName = prompt("mời nhập tên sách"); // nếu k nhập kết quả sẽ là 0

// Kiểu dữ liệu undefined : khi khai báo biến và không gán giá trị
let bookId;

// Kiểu dữ liệu boolean
let isLogin = true;

// Kiểu dữ liệu NaN (not a number);
let result= "nguyễn minh hiển" /3;
console.log("result", result);

